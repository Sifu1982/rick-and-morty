export enum CharacterStatus {
  alive = 'alive',
  dead = 'dead',
  unknown = 'unknown',
}

export enum CharacterGender {
  female = 'female',
  male = 'male',
  genderless = 'genderless',
  unknown = 'unknown',
}
